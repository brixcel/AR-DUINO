using UnityEngine;

public class ewan : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // ewan ko nalang tlga
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
